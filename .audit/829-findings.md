# Integration findings and verified dispositions

The generated Epoch 5 baseline contains 94 tables and 1,331 columns. The audit accounts for all 96 source tables, 1,373 source columns, 49 removed fields, seven additions, and 328 source indexes. Inventory coverage is separate from SQL preparation and runtime qualification.

- Content documents now enforce actual owner/site scope and atomic block replacement. Localized blog documents use resource_localization ownership; same-document parents and cycles are verified. Dependent translations, redirects, placements, and localized documents are deleted through the canonical owner lifecycle.
- Structured hours preserve existing effective schedules. Proven authored/provider timezones are retained or explicitly materialized; four unknown location timezones remain NULL. No historical booking facts are fabricated.
- Timestamp conversions preserve the exact instant. Thirteen malformed canary JSON records are recovered using the proven escaping defect. All 118 known truncated MCP summaries retain every original byte inside valid JSON.
- Eighteen Google-imported published reviews become approved after provenance checks. Existing metadata and review timestamps are preserved; absent structured provider metadata remains NULL.
- Duplicate site domain caches, theme metadata, location Facebook mirrors, unused channel selections, and dead customer fields are removed. Active canonical site_domains and the public template registry own their respective facts. Service uses one vertical value throughout application and storage.
- Better Auth remains authoritative for identity, sessions, OAuth and provider billing identity. Its documented optional defaults are retained; owned current enums, booleans, JSON shapes, scope keys and mandatory counters/timestamps are constrained by the generated baseline.
- Exactly 30 synthetic Markdown media relationships are removed after proving their asset URLs in retained parent Markdown. All 339 media assets and 679 content blocks remain byte-exact. Historical ChowBot records remain intact: 13 conversations and 42 messages, with no active transcript writer or replacement history model.
- Final SQL review found and fixed retired billing projections in three scheduled tasks, a retired QA table reader, and experience fields read from the wrong table in sitemap and module guards. The initial zero inventory-followup count did not detect these; the separate SQL review records that limitation and the corrected preparation evidence.
- NCLS fixture suspicion cleared: all 30 similarly named :media:index placements reference real CTA content blocks. None meets the missing-owner removal condition. Generated fixture SQL passes FK checks and all nine invariants; all 141 fixture assets, 520 blocks and 205 placements are retained.
- The obsolete Epoch 3-to-4 converter, its category planner and sole planner test are removed. The historical runbook links the immutable released source; archived schema and rollback contracts remain intact.

The production-source transfer candidate was independently transformed and verified: 94 tables, 42,244 rows, all projected and unchanged logical hashes matched, and nine ownership/content invariants each returned zero violations. The sanitized transfer summary retains schema/table hashes and counts; raw rows, provider IDs and per-row disposition evidence stay private.

At this audit update, the root agent reports a successful production build, 15 real D1 tests, eight transfer/migration tests, and 187 unit tests across 41 files. These results are attributed to the root agent; this document does not claim independent reruns of those suites. Codex browser, live ingress continuity, remote CI, and staging qualification remain separate release gates.

Production remains on Epoch 4. The owner requested staging qualification only and controls production promotion. Before production switches, the reviewed provider metadata and webhook plan must be applied and verified through the documented canonical procedure. Old reply formats retire under the owner's explicit no-compatibility instruction, while conversation records and unresolved historical facts remain preserved. No live provider mutation is part of staging qualification.
