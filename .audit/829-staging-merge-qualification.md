# Staging merge qualification

This review integrates staging `3894f251d0f846d7a999bed56f3bcd550295bd2f` into PR #848. It records local qualification, not permission to promote or a substitute for exact-head CI and deployed-preview browser checks.

Staging's explicit Localize dialogs, dirty-draft protection, independent editor request generations, shipped Thai interface catalog and session-aware account navigation are retained. Their owners use the consolidated document/product model. Retired database catalog endpoints, page and billing utilities are removed.

The merge fixes the Experiences marketing parent route that intercepted tenant booking routes, the page editor's incorrect version field, pre-hydration remembered-profile clicks, and client navigation validation against the page being left. Translation source identity is relational for every block type. The canonical writer atomically rejects a source type change while translations still reference it.

The normal Drizzle baseline and actual-source transfer are documented in [the consolidation contract](../docs/database/epoch-5-consolidation.md) and [aggregate transfer evidence](829-transfer-summary.json). All nine existing Thai blocks retain exact authored source identity; ambiguous mappings fail the transfer.

Local validation completed on the combined application:

- Full quality chain and production Worker build passed; schema drift, seed and MCP/submission checks passed.
- 223 unit/integration checks passed. Fifteen migration/source-transfer checks passed after the final projection change. The subsequent source-type integrity regression reproduced the defect and passed against real D1, including rollback and explicit translation deletion.
- Full local Chromium suite: 71 passed, one HTTPS-only CIMD private-key/replay case skipped. Two paid media-upload cases were excluded on the command line; test code was not skipped or weakened.
- Codex browser: normal local developer sign-in and direct post-login redirect, Better Auth tenant impersonation with three Today bookings, English source page editor and Thai mobile menu rendering without overflow.

Testing used disposable local fixtures through `local:setup`. A single normal billing-catalog GET read Stripe products/prices into local cache; its temporary credential-bearing Worker was stopped immediately. Subsequent tests used the isolated Worker with a nonfunctional Stripe key and log-only delivery. No OG/social-card generator was run, and no live provider, staging or production mutation occurred.

Remaining release gates: exact pushed-head CI, deployed-preview Codex browser verification, and the canonical staging/cutover process before any production decision.

CI run 34072759693 deployed application head 10548cb1 and completed 65 tests; four workflows hit the default 30-second test deadline. Cloudflare recorded canceled writes at those deadlines, without HTTP 500 responses. The address workflow spent 15.5 seconds navigating; the page-reorder workflow includes four writes and multiple CMS loads. Their budgets are now 90 and 120 seconds respectively. The three complete guest persistence/notification journeys receive 90 seconds. Assertion deadlines, retries, validation-only tests and application code are unchanged.

Focused local verification: four guest tests and the translated-address test passed. A private transport-delay reproduction retained the original contact assertions against the real local Worker: 15 seconds before document delivery plus 15 seconds before POST delivery failed at 30.1 seconds under the old budget, then passed at 31.7 seconds with 90 seconds. This demonstrates deadline cancellation, not the cause of every preview latency interval. Codex browser submissions on the deployed preview returned 201 for contact (9.536 seconds) and reservation (11.927 seconds), and displayed their confirmation pages. No social-card operation was invoked during these checks. Exact-head CI must still confirm the correction.

Before pushing this correction, staging advanced to 27cb96691019ec52bfdadde92bd7cbabaa21d81f (PR #856). Its removal of the local MCP harness merged without conflicts, including package scripts, documentation, the development telemetry endpoint and retired local-domain exceptions. No deleted harness references remain in active callers. All 16 tenant-host tests and lint for the changed host/SEO files passed. Earlier full build and browser results describe the application before this additional staging cleanup; exact-head CI qualifies the combined result.

CI run 34075344450 stopped before deployment or E2E during preview database reset. The old drop planner abandoned dependency ordering when it encountered the requests/reviews/review_requests cycle, placing business_locations before populated requests and violating requests_booking_check through ON DELETE SET NULL. The reset now orders cyclic groups before their external parents and sends the complete small drop sequence in one command, preserving transaction-scoped foreign-key deferral. The temporary-file/50-statement reset batching path is removed; preview identity confirmation, reset sentinel, baseline replay and final checks remain.

The old planner reproduced the exact CHECK failure against a disposable copy containing 53 tables and 18 bookings; the corrected planner reset that same data successfully. A retained real D1 regression populates both booking types and linked review cycles, resets with foreign keys enabled, replays the canonical baseline, and verifies all 53 table names and zero foreign-key violations. The three existing baseline checks and focused lint also pass. Codex read-only local browser verification showed the contact form hydrated without horizontal overflow. No schema migration, remote reset, OG generation or provider mutation was performed during this correction.