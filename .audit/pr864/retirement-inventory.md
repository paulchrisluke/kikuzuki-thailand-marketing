# PR 864 owner-direction inventory

Read the complete release handoff, owner review 5132892027, all three owner inline comments, and the September 7 launch hold. Local branch now includes remote head `bcc55d713a5b60f6725dc65da266690deade67ae`. GitHub reported Ready when this session began. Restored Draft to honor the implementation hold. No qualification watch runs during this batch.

| Obligation | Current state and concrete work | Required evidence and gates |
| --- | --- | --- |
| Retire provider workflows | Implemented locally. Deleted `server/utils/ai-gateway.ts`, the extraction workflow in `chowbot-media.ts`, its MCP registry/executor entries, admin generation endpoint, provider configuration and product promises. Supported media upload and explicit product synchronization remain; their real Worker/MCP journeys passed. | Catalog must omit retired tools; retained media/product journeys must work through the real Worker/MCP boundary. |
| Retire credit charging and exposed balances | Implemented locally. Removed `ai-credits.ts`, `quota-adjustment.ts`, credit endpoints/composable, dashboard billing credit DTO/UI, platform MCP credit outputs, and entitlement/plan credit fields. Credit charging and dead accounting result states are removed from WhatsApp and Google Places callers; subscription authorization and provider operations remain. | Type/lint checks plus real billing, retained Places/WhatsApp, auth and MCP checks. No no-op or zero-balance replacement. |
| Reassess schema and transfer | Implemented locally. `usage_events` has independent MCP telemetry and scheduled Places consumers and must remain. Removed `usage_quota_grants` and obsolete quota snapshots from retained Stripe invoice replay. Canonical schema, generated baseline, seeds, transfer and docs are updated. Live-export transform and verify passed for 52 tables and 42,400 retained rows; all 13 retired grants are counted and hashed. Fresh staging fixtures match all 2,740 rows remotely. | Immutable install and unchanged schema preflight before edits. Regenerate unreleased baseline through Drizzle. Verify declared retired data disposition, retained rows/hashes/schema/FKs and real D1. Old 53-table and staging/rehearsal counts are stale if schema changes. Reprepare an empty unreleased staging candidate through the existing procedure; preserve old production rollback data. |
| Finish MCP/submission audit | Implemented locally. Updated `mcp-tools`, executor dispatch, `scripts/generate-chatgpt-app-submission.mjs`, reviewer cases, `docs/chatgpt-submission-review.md`, generated catalog and root submission JSON. The `sync_products` justification now states its conditional unavailability effect. Final catalog has 96 registry and 95 exposed tools, two removals, 35 retained schema changes and no missing output schemas. | Run both canonical write commands and both check commands. Report final exposed count, submitted-contract diff, missing output schemas, five positive and three negative cases. Owner handles portal resubmission. |
| Prove time formatter fix | Prior evidence exists in `.audit/pr864/ci-34117369033.log`, `observability.json`, local browser logs and checkpoint. Consolidated exact server/client codepoints and Worker correlation in hydration-evidence.md; all 16 Node/Chromium comparisons across 4 locales pass, including 9-digit fractions. | Reuse captured failure evidence, inspect matching Worker diagnostics, verify formatter and affected runtime routes. Prior preview timeout and missed pre-retry Observability ordering remain documented limitations. |
| Optional review tools | CodeRabbit CLI is signed out; its optional review is skipped for this batch. The installed local chatgpt-app-submission skill and official OpenAI documentation supplied the submission audit; the full OpenAI developer plugin is unavailable in this session. | Record actual optional skips or completed audits. Optional tools never waive known defects or mandatory runtime gates. |
| Complete implementation batch | Owner comment 5577976570 is implemented and locally verified. Shared classification, seven demo representations, exact transfer and corrected staging fixtures pass. Runtime fixes restore rich/source save-reload against the unified article contract. See markdown-runtime.md and markdown-transfer.md. Earlier retirement-only results follow. Final build/typecheck, 185 unit tests, 23 D1 cases, 16 epoch tests and guards pass. Rebuilt Worker browser suite passes 72 cases with one HTTPS-only skip. Prior Epoch 6/CMS/demo work is inherited. One coherent commit is being prepared. | Local quality/build, affected unit/D1/epoch tests, browser and MCP evidence, independent diff review, final PR body and completion matrix tied to commit SHA. |
| Qualify and release | Not complete. PR stays Draft. Preview, staging and production qualification remain open. No freeze, production transfer or promotion performed. | Only after all commented work is complete and the user explicitly instructs Ready, watch the exact candidate CI every 30 minutes. Require exact-SHA preview plus manual browser/MCP checks, staging read-only checks, canonical authorized freeze/export/verified epoch cutover, full post-deploy customer browser/MCP/auth/billing checks and main-to-staging synchronization. Keep #829 open until its full definition of done is evidenced. |

The domain shape after retirement retains subscriptions and capability entitlements, explicit product/media operations, and operational usage events. It has no AI-credit balance, credit allowance or quota grant. The workflow uses deletion before additions and real runtime evidence before release. Schema generation and transfer are the highest-risk work; their verification precedes any remote promotion.
