# Epoch 6 block reconciliation

This follow-up addresses owner comments 5580407133, 5580658022 and 5580729953 on top of publication candidate 245f1f2b. The prior retirement, timestamp, publication and preview obligations remain documented in publication-comment-audit.md and publication-local-completion.md. The later correction prohibits new block types and makes residue removal required.

The offline transfer fills missing Markdown modes using the shared classifier, normalizes the observed heading emphasis, removes the exact measured stale block keys, and sets machine-key media alt values to NULL using the owner's predicate. It asserts admitted source shapes, counts the union of changed rows, and preserves unrelated bytes. Canonical fixtures and their generator satisfy the same contract. All six legacy block selectors now name canonical types. The schedule reads its original content through the existing Markdown renderer. Contact uses its existing consultation section and renders card-bearing blocks as a collection.

The retained production-export rehearsal has 52 tables and 42,429 rows. It changes 347 block payloads with one counted overlap, including 264 source-to-rich changes, 48 missing modes, 23 headings and 13 residue rows. All 316 Markdown blocks pass the classifier. Exactly 144 machine-key alt values become NULL and 197 other alt values remain identical. Exact table projections, retained digests, foreign keys and nine invariants pass. A fresh frozen export must be measured and verified again at cutover.

The real CMS journey exposed two separate runtime defects. Compatible Vue dependencies installed two reactive runtimes, so dashboard state changed without updating computed navigation. Canonical Yarn deduplication now installs one Vue 3.5.42 runtime. Four impersonation callers issued competing session refreshes after Better Auth already invalidated its session atom. They now observe the SDK's resulting session before navigation. The tenant realtime connection must close before identity changes and remain closed through exit navigation.

| Verification | Result and evidence |
| --- | --- |
| Quality and typecheck | Passed after the SDK session and synchronous socket-close changes. Any later source change requires its own check. |
| Unit / real D1 / migration and transfer | 185 / 23 / 17 passed. The transfer suite includes nine cases. |
| MCP catalog, submission and contract diff | Passed. Existing publication contract evidence remains applicable. |
| Retained browser suite | 90 passed, one HTTPS-only skip, zero retries on build 81b26fb7. See residue-retained-browser-proof.json. |
| Affected public routes | All 16 desktop/narrow cases passed on build ab22e07e. See residue-affected-route-proof.json. |
| Plain heading HTTP rendering | All 23 headings on three routes matched with zero literal emphasis markers on ab22e07e. See block-heading-local-after.json. |
| CMS save, reload, restoration and Exit to Admin | Passed the strict console and network gate with no socket-warning exemption on build bb52bb9e. The exact original block-data hash was restored; Exit reached the admin page with the banner gone and created no socket after stop began. Both rapid-exit probes passed. The pointer path closed an open socket silently. The keyboard path deliberately closed one tracked connecting socket and produced exactly its correlated browser-native cancellation warning, with no post-stop connection or other failure. |
| Reconciled staging D1 | b6505079-9408-475c-a669-420504165367 has 52 tables and 2,816 pristine fixture rows, verified by remote re-export. See reconciled-staging-summary.json. No Worker deployment. |
| Independent source and comment reviews | No demonstrated source omission or suppression. See residue-obligation-review.md and residue-final-comment-review.md. |

The browser evidence is composite and explicitly names each build. The earlier 544-case publication sweep and its affected follow-ups remain historical evidence; this record does not claim a new full 544-case pass. Browser runs use one page and one worker within the user's three-tab limit.

#868 owns shared field-format enforcement, per-block alt writer/renderer removal, blog publishing controls and the image slash-menu correction. Its rebase must consume this backfill and the inseparable fixture/public-consumer corrections. Real media alt descriptions are separate work. The missing-description client handoff gate is intentionally not waived.

All source and local verification gates are complete. The 08:29:48 UTC audit compared all 30 current comment/review bodies and found no changes or unaddressed source work. Exact-candidate preview, staging Worker qualification, the authorized production freeze/export/verified transfer/cutover and deployed checks remain release gates. Production remains Epoch 5, rollback resources are retained, and #829 stays open. The owner handles ChatGPT portal resubmission.
