# Block contract reconciliation

Current scope follows owner comments 5580407133, 5580658022 and 5580729953. The last correction explicitly prohibits new block types and requires residue deletion in this epoch. The proposed scheduling type, editor controls and generated schema changes were reverted before push. The database prepared for that proposal remains retained and undeployed. It does not qualify this batch.

1. Read the principles and every current owner body. Complete. Delete/reuse/modify ordering removed the proposed type and reused canonical Markdown. The 07:37:42 UTC refresh found all 30 current comment objects unchanged.
2. Ground and sketch exact source projections. Complete. Extend byte-preserving root-token edits instead of reserializing entire payloads. Exact source key sets and discriminators gate residue removal. The scheduling row's existing content string moves unchanged into markdown; obsolete structured fields are deleted as requested.
3. Implement and verify the offline transfer. Complete. Nine integration cases and full retained-export transform/verify pass. The export retains 52 tables and 42,429 rows. Its 347 changed block payloads cover 264 source-to-rich changes, 48 missing modes, 23 headings and 13 residue rows with one explicit overlap. Exactly 144 machine-key alt values become NULL and the other 197 alt values remain identical. All 316 Markdown rows satisfy the shared classifier.
4. Correct canonical fixtures and required consumers. Complete. Fixtures have their own measured census. NCLS changes only 141 media alt values and 46 block payloads. Canonical generation supplies editor modes and stops writing image alt. All six legacy block lookups now use explicit canonical types; duplicate singleton matches fail. Contact renders its card rows as a collection and selects its CTA through its existing consultation section. Scheduling uses the existing Markdown renderer. The filter on deleted legal_meta data is removed.
5. Run combined local verification and prepare a fresh staging candidate. Complete. Quality and typecheck, 185 unit tests, 23 real D1 tests, 17 migration tests, and the migration, catalog, submission and contract checks pass. The retained browser suite passed 90 cases with one HTTPS-only skip and no retries on build `81b26fb7-9376-43e4-9ef2-e28d5d0bb007`. A fresh 16-case affected public-route run and all 23 corrected-heading HTTP checks passed on final build `ab22e07e-d138-4e94-bac3-30a36031bef1`. The normal CMS save, reload, exact restoration and Exit-to-Admin workflow passed its strict gate without socket-warning exemptions on build `bb52bb9e-51b2-4843-be1d-561ece4ad3a4`. Both rapid-exit probes passed: the pointer path closed an open socket silently, while the keyboard path deliberately closed one tracked connecting socket and observed exactly its correlated browser-native cancellation warning, with no post-stop connection or other failure. The final empty APAC candidate passed generated-baseline qualification and exact 52-table, 2,816-row import/re-export parity.
6. Reconcile current comments, publish the completed batch, and then mark Ready. Local gates are complete. The 08:29:48 UTC refresh compared all 30 current comment/review bodies and found no changes. Refresh all current comment bodies again before pushing and immediately before Ready. Never use an old ID-only audit as the completion gate.

#868 at 7dffdd1e owns the shared format registry and writer enforcement, per-block alt renderer/editor removal, blog publishing controls and image slash-menu correction. Its shared writer is now server/utils/content/documents.ts. The epoch backfill must land before or with enforcement. The fixture generator corrections overlap deliberately so this PR's pristine fixtures already satisfy the new contract; #868 can reuse them on rebase.

Seven application-only files changed after the staging snapshot: `yarn.lock`, `composables/useAuth.ts`, `composables/useDashboardInvalidations.ts`, `layouts/dashboard.vue`, and the three admin pages. They deduplicate the Vue runtime, wait for Better Auth's session signal, and synchronously clear the tenant realtime scope during impersonation exit. They do not change fixtures, schema, transfer logic or the shared Markdown classifier. Their final Worker runtime proof is separate from the qualified database candidate.

Real content-image alt descriptions are separate media-pipeline work. NULL values must remain visible to the client verification gate. Do not substitute adjacent titles or captions, silence the gate, or claim client handoff is complete.

Production remains on Epoch 5. Exact-candidate preview qualification, staging Worker deployment and the previously authorized production freeze/export/verified transfer/cutover remain release gates in the canonical operating documents. No custom migration or manual tenant-data repair is part of this work.
