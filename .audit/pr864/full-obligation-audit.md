# PR 864 full obligation audit

Audited 2026-09-08 against committed candidate `c38d16fbc74b0d8fc231548b1e5c8015f7326a60`. The inputs were the complete issue-comment, inline-comment and review snapshots in `.tmp/pr864-all-comments.jsonl`, `.tmp/pr864-all-inline.jsonl` and `.tmp/pr864-all-reviews.jsonl`, plus the downloaded handoff at `.tmp/pr864-original-handoff.md`. The complete linked handoff in comment `5570026523` was read from the issue-comment snapshot. This audit made no GitHub, CI, browser, deployment or database action.

The PR is Draft. The user's latest instruction authorizes one Ready transition after all implementation/comment obligations and local verification are complete, committed and reported at the final SHA. Until then, keep Draft. Evidence from `1dace0fe` and `62931b62` remains useful historical proof, but it does not qualify `c38d16fb` or a later candidate.

## Owner obligations

| Source | Obligation | Current evidence | State |
| --- | --- | --- | --- |
| Review `5132892027` | Read the complete handoff and owner comments before another qualification push. Complete the main body as one batch. | Comment `5576660591` inventories the handoff and all three inline comments. Retirement, owner follow-up and Markdown changes are now committed through `c38d16fb`. | Implementation done. Do not make another Ready transition until every pre-Ready item below is complete. |
| Comment `5570026523` | Keep #864 as the sole release PR and retain the already consolidated #865/#866 work. Do not promote an older SHA or merge the superseded PRs separately. | `741fd6b9` and `6108872a` are ancestors of `c38d16fb`; #864 contains the CMS and demo work. | Done in source. |
| Comment `5570026523` | Use canonical source, generated schema and the documented epoch procedure. Do not add compatibility paths, custom migrations or production patches. Preserve Better Auth, customer data and rollback resources. | `server/db/schema.ts` remains authoritative; the generated 52-table baseline and `scripts/epoch6-data.mjs` are used. Released archives are unchanged. `docs/database/epoch-6-cutover.md` retains old resources. | Done in source and rehearsal. Must be followed again during cutover. |
| Comment `5570026523` | Maintain and verify MCP catalog/submission artifacts. Use the optional OpenAI developer plugin when available, but do not operate the portal. The owner handles resubmission. | Canonical catalog/submission generators and checks passed. Current contract is 96 registry tools, 95 exposed, two removed, 35 retained schema changes, no missing output schemas, and five positive/three negative cases. The full plugin was unavailable; the local submission skill and official documentation were used. | Done locally. Deployed contract comparison remains a post-deploy gate. Portal resubmission remains owner-only. |
| Comment `5570026523` | Use optional CodeRabbit CLI if installed and accepted. Record a real skip if unavailable or signed out. | `.audit/pr864/completion-matrix.md` and `.audit/pr864/pr-body.md` record that CodeRabbit was signed out and skipped. | Properly skipped. It is not a release blocker and cannot substitute for source/runtime review. |
| Comment `5570026523` | Diagnose prior preview hydration and localization failures from concrete output and Observability before retrying. Do not suppress warnings or weaken waits. | `.audit/pr864/hydration-evidence.md`, `.audit/pr864/observability.json`, `.audit/pr864/ncls-local-timeout.md` and the clean local browser evidence preserve the failures and diagnoses. The NCLS document completed; external media requests remained pending and no single asset was established as the root cause. | Done for the recorded failures. New exact-head failures require the same diagnosis process. |
| Comment `5570026523` | Qualify exact preview, then staging, then the authorized production epoch cutover and post-deploy checks. Keep #829 open until its full definition of done is evidenced. | No exact-head preview run exists for `c38d16fb`. The staging data resource is prepared but has no Worker deployment. Production remains on Epoch 5; no freeze or cutover occurred. #829 remains open. | Open release work. See the gate sections below. |
| Comment `5572050864`, item 1 | Publish an obligation inventory and keep implementing. | Comment `5576660591` supplied the inventory and implementation continued through `c38d16fb`. | Done. |
| Comment `5572050864`, items 2 and 3; inline `3950485039` | Retire AI Gateway analysis/extraction and all AI-credit charging, gates, balances, grants, endpoints, tools, UI and promises. Do not retain a shim or fabricate an importer. Preserve subscriptions, explicit products/media, Places, WhatsApp, auth and operational usage. | Commit `1dace0fe`, `.audit/pr864/retirement-inventory.md`, `.audit/pr864/retirement-contract-diff.json` and `.audit/pr864/retired-api-runtime.json` document deletion and direct retired-tool/route failures. Repository search finds retired names only in historical audit/cutover evidence, the transfer disposition and guards. | Done in source. Retained provider/auth/billing behavior still needs deployed qualification. |
| Comment `5572050864`, item 4 | Reassess schema and transfer after credit retirement. Regenerate the unreleased baseline, account for retired rows and prepare a fresh staging candidate. | The baseline contains 52 tables. `usage_quota_grants` is a declared retired table; `usage_events` remains. The retirement rehearsal preserved 42,400 rows and counted/hashed 13 grants. The Markdown rehearsal now preserves 42,429 rows and reclassifies 264 blocks. Corrected staging D1 `aa9db76b-b698-4c92-8cf3-c140321a064c` re-exported 52 tables and 2,816 fixture rows with schema, FK, integrity and invariant parity. | Done for rehearsal and staging data preparation. Counts are not frozen-production evidence. |
| Comment `5572050864`, item 5; inline `3950499799` | Re-audit the final MCP/submission contract and make `sync_products` describe its conditional omission effect. | `server/utils/mcp-tools/products.ts` states that omission changes availability only when `set_missing_unavailable` is true. Generated artifacts and contract diff pass. Reply `3953449951` records the correction. | Done locally. Production catalog comparison remains open. |
| Comment `5572050864`, item 6; inline `3950491540` | Justify or remove the timezone whitespace regex with captured values, code points, canonical-boundary rationale and localized precision proof. | `.audit/pr864/hydration-evidence.md` records U+202F server versus U+0020 browser output and the shared formatter callers. `.audit/pr864/time-format-runtime.json` records 16 exact Node/Chromium comparisons across four locales with seconds and nine fractional digits. Reply `3953449840` reports the same scope. | Done. Exact-head preview hydration remains a release gate. |
| Comment `5572050864`, item 7 | Batch known work and local validation before the next qualification push. Cancel superseded CI and do not watch while Draft. | Retirement and follow-up runs were split as new owner scope arrived. The premature Ready attempt for `62931b62` was cancelled and Draft restored. Comment `5578478488` correctly keeps `c38d16fb` Draft. `.audit/pr864/full-local-completion.md` now records the combined local validation. | Local batch and evidence complete. Commit and report the final SHA before the user-authorized Ready transition. |
| Comment `5577976570`, item 1 | Use one dependency-free Markdown predicate in validation, splitter, seeds and transfer. Preserve source mode for tables/raw HTML and rich mode for ordinary prose/empty content. | `shared/markdown-editor-mode.ts` is imported by validation, the shared splitter, Pottery seed and transfer. Demo uses the shared splitter. The NCLS reclassification script imports the same predicate. | Done. |
| Comment `5577976570`, item 2 | Correct all affected NCLS, demo and Pottery seed values with the shared classification. | `seed-definitions/ncls.ts`, `seed-definitions/demo.ts` and `seed-definitions/pottery-house.ts` are corrected. `.audit/pr864/demo-markdown-probe.json` and staging re-export verify fixture shape. | Done. |
| Comment `5577976570`, item 3 | Split demo articles into real heading/prose/image blocks and remove duplicate opening H1s. | Seven English/Thai representations contain 8 to 12 blocks, seven explicit content-block media placements reuse four existing assets, source links align, and built public pages rendered headings and loaded images. | Done. |
| Comment `5577976570`, item 4 | Backfill production only through the epoch transfer. Change only the top-level `editor_mode` token, record counts and fail verification on any other byte change. | `.audit/pr864/markdown-transfer.md`, `scripts/epoch6-data.mjs` and `tests/integration/epoch6-data.test.mjs` show 268 source blocks, four retained and 264 reclassified. Exact projection and deliberate-corruption rejection passed across 52 tables and 42,429 rows. | Rehearsal done. Must be repeated from the frozen export during cutover. |
| Comment `5577976570`, item 5 | Record the content transformation and measured counts in the Epoch 6 cutover contract. | `docs/database/epoch-6-cutover.md` records authorization, predicate, exact-byte rule, rehearsal counts and the corrected staging resource. | Done. |
| Comment `5577976570`, verification | Re-run census/transform/verify and prove the editor, seeds and public rendering through real boundaries. | Sixteen migration tests, two focused real-D1 tests, a 16-case actual platform-content D1 probe, built CMS rich/source save/reload, root-relative canonical save, and seven demo representations passed. Runtime fixes for nested Vue proxies, mount normalization and the obsolete second version token are included in `c38d16fb`. | Done locally. Deployed exact-head coverage remains open. |

## Non-owner review obligations

| Source | Finding | State |
| --- | --- | --- |
| CodeRabbit review `5129698559`, inline `3947871619` | `update_location.notification_phone` must accept `string` or `null` and explain that null clears only the location recipient. | Done in `server/utils/mcp-tools/shared.ts` and regenerated tenant catalog. |
| CodeRabbit issue comment `5567604101` | Its actionable source finding is inline `3947871619`. Its title/body/docstring warnings describe an earlier `fd87da1d` snapshot and generic bot policy. | Source finding done. The comment is not a review of `c38d16fb`; do not claim current CodeRabbit coverage. |

## Current comment and review ledger

| Record | Meaning after this audit |
| --- | --- |
| `5570026523` | Owner handoff. Its 53-table, 98/97-tool and old staging/rehearsal numbers are explicitly superseded by `5572050864` and `5577976570`. Its release sequence and safeguards remain binding. |
| `5572050864` | Owner retirement hold. Source work is done; exact-head qualification and release are open. |
| `5577976570` | Owner Markdown addition. Source and local runtime work are done; frozen-source execution and deployed qualification are open. |
| `5132892027` | Owner review directing complete-batch work. It remains COMMENTED, not an approval. The PR remains Draft. |
| `3950485039`, `3950491540`, `3950499799` | Owner inline findings. Each is addressed in source and has a scoped reply (`3953449726`, `3953449840`, `3953449951`). |
| `5576660591` | Initial retirement inventory. Its pending rows are historical and superseded by later implementation comments. |
| `5577348749`, `5577394499` | Retirement-only completion and independent local runtime evidence for `1dace0fe`. Valid for that SHA only. |
| `5577643910` | Correctly records cancellation of the incomplete `1dace0fe` qualification and restored Draft hold. |
| `5577969608` | Local follow-up evidence for `62931b62` is useful. Its final sentence announcing another Ready transition was premature and is superseded by the user's correction and `5578478488`. It must not be used as authorization or current status. |
| `5578478488` | Current Markdown completion comment for `c38d16fb`. It correctly limits the claim to local implementation and leaves release gates open. |
| Reviews `5136245699`, `5136245927`, `5136246031` | Empty collaborator review shells associated with the three inline replies. They add no approval or obligation. |

## Pre-Ready completion status

1. Combined local validation is complete. `.audit/pr864/full-local-completion.md` records 185 unit cases, 23 serial D1 cases, 16 migration cases, catalog/submission checks, final quality/build, and the full built-Worker browser run with 84 passes, one HTTPS-only skip and zero retries. `.audit/pr864/full-ncls-desktop.log` adds the corrected 1280x720 NCLS journey rerun with one pass and zero retries. The viewport correction was independently reviewed and preserves the route, status, warning/error and content assertions.
2. The final E2E resource changes were independently reviewed for assertion preservation and the three-tab constraint. Comment Sicko returned zero findings; the no-comments and deslop review found no comments, suppressions, retries or unrelated cleanup to remove.
3. Keep the PR Draft until these test and audit records are committed and the resulting exact SHA is inserted in the combined completion record and reported. The user's latest instruction then authorizes one Ready transition without another permission turn. Repin the matrix and independent review if the committed candidate changes.

No unresolved owner-comment implementation or local-validation defect is demonstrated in the completed batch. The remaining pre-Ready work is to commit, pin and report the final SHA, not to add another API, fallback, schema change or fixture.

## Gates after completing pre-Ready work

1. After the complete pre-Ready inventory passes, mark the final SHA Ready once as requested by the user. Start the 30-minute CI cadence for its qualification run. Do not reuse cancelled run `34174370534`, the premature `62931b62` run, or public preview checks for `1dace0fe`.
2. Require Checks, build, preview deployment, fixture verification and the selected E2E suite to complete. Preserve request IDs, Ray IDs and timestamps for any failure; inspect matching Worker Observability before a diagnostic retry.
3. While CI runs, verify the exact deployed build and assets, then cover all release-required Pottery House, Kikuzuki and NCLS public routes on desktop and narrow layouts. This PR changes shared content, date/time, navigation and media models, so the expanded sitemap/fixture route set applies.
4. Exercise preview-disposable authenticated CMS and MCP flows affected by the complete PR. Include OAuth discovery/PKCE/token exchange, `initialize`, `tools/list`, workspace/site reads, post/location/hours/availability/content mutations, Markdown rich/source save/reload, feature labels, and retained explicit product/media behavior. Verify the deployed catalog has 95 exposed tools and lacks both retired tools.
5. Treat Dark Reader-injected style warnings as a documented browser-profile limitation only when the matching server response remains clean and extension-free Chromium passes. Treat any first-party hydration, localization, media or save failure as blocking until diagnosed.

## Staging gates

1. Merge the qualified PR to `staging` through the branch-driven flow. Confirm the Worker uses `krabiclaw-db-staging-epoch6-editor-mode`, ID `aa9db76b-b698-4c92-8cf3-c140321a064c`. Do not reinitialize a prior 53-table or retirement-only resource.
2. Complete the documented post-start initialization for the unreleased staging candidate, including generated social cards where the runbook requires them. The current evidence proves data parity before Worker start; it does not prove runtime initialization.
3. Run read-only staging customer aliases and tenant MCP OAuth/content checks. Verify Better Auth membership/permissions, retained Places and WhatsApp paths, billing status and subscription behavior, public routes, dates, schedules, closures, media and localized content. Do not infer paid billing continuity from the synthetic demo customer, whose Stripe ID returns `resource_missing`.
4. Open or update the ordinary `staging` to `main` PR for the exact qualified staging SHA. Do not create another deployment or epoch mechanism.

## Production cutover and completion gates

1. Deploy the maintenance build once with the old production D1 binding and `DB_WRITE_FROZEN="true"`. Verify HTTP, email, queue and scheduled work are blocked/deferred as documented, then drain at least 60 seconds.
2. Take a fresh private frozen Epoch 5 export. Recount every table and row. Re-run Markdown eligibility, retired-grant disposition, timestamp projection, exact retained/projected hashes, schema, indexes, checks, foreign keys and all domain invariants. Rehearsal totals of 42,429 rows, 268 source blocks and 264 reclassifications are not cutover totals.
3. Apply only the generated baseline to the empty production candidate `c38458ef-eb4f-4bc2-8922-13cec27237dd`, import the verified data-only payload, re-export it and prove exact projection. Preserve the old production database and released history for rollback.
4. Promote the normal Epoch 6 binding through the ordinary `main` release and remove the freeze. If cutover cannot complete promptly, restore the prior Worker through Cloudflare deployment history rather than patching data.
5. Wait for exact-build convergence on all customer domains and referenced assets. Run the full read-only production browser and MCP matrix, including all published routes affected by shared content/date/media changes, OAuth and tenant reads, Better Auth, billing/subscription continuity, Places, WhatsApp and other retained provider paths. Production writes or notifications require the explicitly authorized self-test canary and verified recipient; never use a client recipient.
6. A real paid billing path remains unverified because the local synthetic customer is absent from Stripe. Obtain read-only evidence from an authorized real account or record the external-access blocker. A login page is not authentication evidence.
7. The owner handles ChatGPT portal resubmission. Supply the final deployed JSON/commit and findings. Any required real ChatGPT-host discovery, selection or host-argument check must use the deployed contract and owner-supported access; direct MCP tests do not prove host behavior.
8. Record final deployed SHAs, CI links, bindings, browser/MCP results and limitations in #829. Close it only after its complete definition of done, production verification and rollback state are evidenced. Sync the released `main` result back to `staging`, including any authorized hotfix.

## Claims that must not be carried forward as current

- `53 tables`, `2,863 staging rows`, `42,211 rehearsal rows`, `98 registry tools` and `97 exposed tools` are superseded historical values from comment `5570026523`.
- The `1dace0fe` preview deployment, 21 local independent cases, 10 remote public cases, 72 local browser cases and cancelled CI run do not qualify `c38d16fb` or a later SHA.
- `.audit/pr864/issue829-progress.md` says exact-candidate preview qualification started for `1dace0fe`; that run was cancelled and the file is stale as a current status source.
- `.audit/pr864/follow-up-completion.md` ends by announcing a Ready transition for `62931b62`; the user later ruled that transition premature. Its local checks remain evidence, but its release action is superseded.
- The current staging D1 is prepared data, not a qualified staging Worker. The production candidate is empty, not migrated production.
- Optional tool skips are properly recorded. They do not count as successful CodeRabbit or OpenAI developer-plugin review.
